# تحديث غرفة Saki Chat

## السلوك الجديد

أصبح زر الرجوع في Android وزر الخروج داخل الغرفة يعرضان نافذة منبثقة شفافة مستديرة تحتوي على خيارين رأسيين: «خروج من الغرفة» و«حفظ الغرفة». اختيار الخروج يفتح تأكيدًا ثانيًا؛ وبعد التأكيد فقط يتم النزول من المقعد، إنهاء قناة Agora، إنهاء عضوية المستخدم في `voice_room_members`، وإيقاف خدمة الخلفية. اختيار الحفظ لا ينفذ `leaveRoom` ولا `leaveSeat` ولا `leaveChannel`.

عند الحفظ، تبقى `RoomPage` موجودة داخل `HomeShell` ولكنها تصبح مخفية بصريًا، وتظهر فقاعة دائرية تحمل صورة الغرفة فوق صفحات التطبيق. الضغط على الفقاعة يعيد الغرفة فورًا مع استمرار حالة الجلسة والمقعد والاتصال الصوتي.

## Android

أضيفت `RoomAudioService` كخدمة foreground من نوع microphone مع إشعار دائم، كما أضيف `SYSTEM_ALERT_WINDOW` لفقاعة الظهور فوق التطبيقات، و`POST_NOTIFICATIONS`، و`READ_MEDIA_AUDIO`، و`READ_MEDIA_IMAGES`، و`READ_MEDIA_VIDEO`، وامتدادات التخزين للإصدارات القديمة. أضيفت MethodChannel بين Flutter وKotlin لبدء الخدمة وإيقافها وإظهار الفقاعة وإخفائها وفتح الغرفة عند الضغط عليها.

أضيفت أزرار داخل معلومات الغرفة لطلب أذونات الموسيقى والملفات والصور والفيديو، وطلب إذن الظهور فوق التطبيقات. تم الحفاظ على Agora الحالي وإدارة التوكن الموجودة، مع إبقاء `AgoraAudioService` خارج دورة التصغير وعدم استدعاء `dispose` عند الحفظ.

## التصميم

تمت إعادة تصميم غرفة الدردشة بخلفية Teal ليلية وتدرجات أزرق/أخضر، ورأس شفاف بنص أبيض وتاج ذهبي، ملخص غرفة شبه شفاف، فقاعات دردشة داكنة، وشريط أدوات سفلي داكن بأيقونات بيضاء وذهبية. توسعت إعدادات المالك لتشمل الاسم والإعلان والوسوم والخلفية ومظهر الميكروفون وعدد المقاعد والسماح بالتحدث وتشغيل الميكروفون تلقائيًا، وتحفظ القيم داخل `voice_rooms.metadata.settings` عبر الدوال الحالية.

## التحقق

تم تنفيذ `flutter analyze` بنتيجة `No issues found`، و`flutter test` بنتيجة `1 test passed`. تم بناء Web Release وDebug APK، وفحص APK للتأكد من صلاحيات الميكروفون والظهور فوق التطبيقات والخدمة الأمامية ووسائط Android. لا يوجد جهاز Android متصل في بيئة الاختبار، لذلك تم التحقق من APK وManifest والبناء، بينما يحتاج اختبار اللمس الفعلي للفقاعة والصوت في الخلفية إلى تثبيت APK على جهاز Android حقيقي.

## GitHub

تم رفع التحديث إلى `main` في commit `be8b3e391996590ac58edfe8a5ba79402c8ecf54`.
